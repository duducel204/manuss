name: Shell tests

on:
  push:
  pull_request:

jobs:
  termux-scripts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate shell scripts and simulated installation
        run: bash tests/test_termux_installation.sh
